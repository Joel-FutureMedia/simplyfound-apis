package com.example.simplyfoundapis.repositories;

import com.example.simplyfoundapis.models.HeroSection;
import org.springframework.data.jpa.repository.JpaRepository;

public interface HeroSectionRepository extends JpaRepository<HeroSection, Integer> {
}
